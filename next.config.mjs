/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 't3.ftcdn.net', 'tacit-mallard-383.convex.cloud', 'upload.wikimedia.org'],
    },
};

export default nextConfig;
